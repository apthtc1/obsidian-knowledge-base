sudo apt install cifs-utils --- для монтирования
sudo apt install rpm
sudo apt install gcc
sudo apt install make
sudo apt-get install linux-headers-$(uname -r)

sudo apt install flex
sudo apt install bison

Пакеты для настройки secure-boot 
sudo apt astra-secureboot (этот пакет скорее всего сразу установлен, поэтому его может и не быть)
sudo apt astra-safepolicy 
sudo apt efitools 
sudo apt sbsigntool 
sudo apt uuid-runtime